# 第 1-12 题

## 1. 请分享一个你主导的 Agent 项目，遇到了什么技术挑战？

淘工厂私域 ChatBot AI 导购。从 0 到 1 基于 LangGraph 搭建，覆盖商品推荐、售前咨询、搭配购买。

**核心挑战：**
- **意图识别准确率低**：电商用户表达模糊（"有没有便宜点的""那种厚一点的"），初版意图分类只有 60% 准确率。通过 SFT 微调 + Prompt 精简 + 四维评测体系（测试集/代码规则/人工标注/LLM 多维打分）迭代到 87%。
- **RT 过高**：Agent 链路涉及意图分类 → Plan 生成 → 多工具调用 → 结果聚合 → 回复生成，初版 RT 2.5s。通过 KV Cache、并行召回、Plan 粒度优化降至 300ms 以内。
- **知识库质量**：历史客服语料噪声大，直接做 RAG 召回率低。设计了"预定义类别 → 分类提取 → 生成标准 Q&A"的结构化挖掘流程沉淀高可信知识库。

## 2. 在团队合作中，如果成员对技术方案有分歧，你会怎么处理？

**原则：用数据和实验说话，而不是用职级和声量。**

具体做法：
1. **先对齐目标**：分歧通常源于对目标的理解不同。先确认大家对"好"的定义一致（是性能优先、成本优先还是开发速度优先）。
2. **各自写 one-pager**：让每个方案的支持者用一页纸写清楚方案的优劣和适用场景，避免口头讨论跑偏。
3. **小规模验证**：如果争议核心是性能/效果，花 1-2 天做 POC 对比，数据面前分歧自然消解。比如导购项目中，关于 RAG 用 HyDE 还是 Multi-Query，我们各花半天实现 demo，用测试集跑完效果对比就有了结论。
4. **决策归档**：最终方案和决策理由写入文档，避免后续重复争论。

## 3. 描述一次你通过数据分析优化 Agent 性能的经历。

导购 Agent 上线初期，A/B 实验显示推荐转化率低于预期。

**分析过程：**
1. 埋点分析发现 40% 的推荐结果用户没有点击，抽样看 case 发现推荐商品与用户意图偏差大。
2. 拆解召回链路数据：语义向量召回相关性 OK，但 Text2SQL 召回的商品属性匹配率只有 55%——原因是用户口语化表达（"显瘦的""不起球"）与商品结构化属性对不上。
3. 引入相关性判别模型在召回后精筛，过滤掉低相关性结果；同时在 Text2SQL 前加一层 query 改写，将口语化表达映射到标准属性词。
4. 改进后召回相关性从 55% 提升到 82%，推荐点击率提升 35%。

**关键：不是笼统地"优化模型"，而是先用数据定位瓶颈在召回环节的哪一路，再针对性修复。**

## 4. 当项目进度紧张时，你是如何确保代码质量的？

**核心策略：分层保障，关键路径高标准，非关键路径适当妥协。**

- **主链路强保障**：核心 Agent 编排逻辑、工具调用接口必须有单测和集成测试，CR 不能省。
- **非主链路快速迭代**：日志、监控、管理后台等允许先实现功能后补测试。
- **代码规范自动化**：lint + format 预提交钩子，减少人工 review 低级问题的时间。
- **技术债记录**：赶进度时的 workaround 写 TODO 并建跟踪 issue，不是写完就忘。
- 实际例子：导购项目一期赶上线时间，我把知识库管理后台的前端做了最简版本（纯表格 CRUD），但 Agent 核心链路的异常处理和降级逻辑一行没省。

## 5. 请举例说明你如何将用户需求转化为 Agent 功能设计。

**场景：** 产品提出"用户问搭配推荐时，Agent 能推荐一套穿搭"。

**转化过程：**
1. **拆解需求本质**：用户说"帮我搭配"时，期望的是多品类商品组合（上装+下装+鞋），不是单品推荐。
2. **定义 Agent 能力边界**：搭配推荐需要跨品类召回 + 风格一致性判断 + 预算约束，单个工具做不了。
3. **设计为复合 Plan**：Planner 生成 [(品类识别, tool), (各品类召回, tool), (风格匹配过滤, tool), (组合排序, tool), (话术生成, agent)] 的执行序列。
4. **定义验收标准**：搭配结果需满足——品类不重复、风格一致（通过属性标签交叉验证）、总价在用户预算 ±20% 以内。
5. **灰度验证**：先在 10% 流量开放，收集用户点击率和购买转化率作为反馈信号。

## 6. 你如何评估一个 Agent 系统的成功与否？

**分层评估：**

**效果层（最重要）：**
- 业务指标：GMV 增量、购买转化率、客单价（我们的 A/B 结果：日均增量 GMV +20 万）
- 用户体验指标：推荐点击率、对话轮次（越少越好，说明理解准确）、用户满意度评分

**质量层：**
- 意图识别准确率 / 精确率 / 召回率（87% / 93% / 92%）
- 知识库召回准确率
- 幻觉率（回答与事实不符的比例）

**工程层：**
- RT P99（300ms 以内）
- 可用性（99.9%+）
- 成本（单次对话的 token 消耗和推理成本）

**关键原则：** Agent 系统的成功不是"能对话"，而是"能产生业务价值"。技术指标是过程指标，业务指标是结果指标。

## 7. 遇到技术难题时，你通常的解决思路是什么？

**四步法：定位 → 拆解 → 验证 → 沉淀**

1. **精确定位问题**：不急于找解法，先确认问题到底是什么。用数据/日志/复现路径定位到最小范围。
2. **拆解为子问题**：大问题拆成可独立验证的小问题。比如"RT 太高"拆成"意图分类耗时 + Plan 生成耗时 + 工具调用耗时 + 回复生成耗时"，逐段计时找瓶颈。
3. **最小化验证**：对每个子问题设计最小实验验证假设，避免大改后发现方向错误。
4. **沉淀复用**：解决后写复盘文档，把方法论沉淀下来。

实际例子：反向 HyDE 的引入——先发现售前咨询 RAG 召回率低 → 分析发现用户 query 和知识库文档在语义空间差距大 → 调研 HyDE 思路但发现正向 HyDE 不适用（生成假文档质量不稳定）→ 反向思考将检索空间迁移到 query 侧 → 小规模验证有效 → 全量上线。

## 8. 请分享一次你主动学习新技术并应用到工作中的经历。

LangGraph 从调研到落地。

2024 年初项目启动时 LangGraph 刚发布不久，团队没人用过。我花两周时间：
1. 通读 LangGraph 源码和文档，理解 StateGraph、Checkpoint、Human-in-the-loop 等核心概念
2. 在本地搭 demo 验证其是否满足我们的需求（多 Agent 编排、状态管理、条件路由）
3. 对比 AutoGen、CrewAI 等方案，最终选定 LangGraph（原因：状态管理精细、支持 re-plan、可控性强）
4. 在此基础上设计了我们的意图路由 + Plan & Execute 架构，并推动团队采纳

**关键：不是为了学而学，而是带着明确的问题（"怎么实现可控的多 Agent 编排"）去选型和学习。**

## 9. 在跨部门协作中，如何确保技术方案被业务方理解？

**策略：翻译 + 可视化 + 对齐利益。**

1. **语言翻译**：不说"向量检索 + re-rank"，说"让机器理解用户想要什么，从几十万商品里找到最匹配的"。
2. **效果 demo**：做一个可交互的 demo，让业务方亲自试，比 PPT 讲一百遍都有效。导购项目启动时，我搭了一个简易对话界面让产品和运营亲自提问，5 分钟内他们就理解了 Agent 能做什么不能做什么。
3. **对齐利益**：把技术方案翻译成业务方关心的指标。"用 Plan & Execute 架构" → "能同时处理简单和复杂问题，简单问题快、复杂问题准"→ "预计转化率提升 X%"。
4. **风险透明**：明确告诉业务方哪些是确定能做到的，哪些有不确定性需要实验验证，管理预期。

## 10. 描述一次你优化 Agent 响应速度的具体做法。

**背景：** ChatBot 导购初版 RT 2.5s，用户体验差。

**优化手段（逐层）：**

1. **意图分类提速**：从通用大模型换为 SFT 后的小模型，意图分类 RT 从 800ms 降到 50ms。
2. **KV Cache**：对高频 system prompt 和 few-shot 部分启用 KV Cache，避免每次重复计算，Plan 生成 RT 降 40%。
3. **并行召回**：商品推荐的四路召回（向量/Text2SQL/行为序列/Graph RAG）从串行改为并行执行，耗时从四路之和降为最慢一路。
4. **Prompt 精简**：砍掉冗余指令，将 prompt token 数从 2000+ 压缩到 800 以内，直接降低 LLM 推理耗时。
5. **流式输出**：回复生成改为 streaming，用户感知的首字延迟大幅降低。

**最终效果：** RT 从 2.5s 降至 300ms 以内。

## 11. 当需求频繁变更时，你如何保持开发效率？

**核心：架构上预留变化空间，流程上管理变更成本。**

- **架构层面**：意图路由 + 可插拔的子 Agent 设计。新增一个业务场景（比如新增"退换货咨询"），只需新增一个子 Agent + 在路由层加一条规则，不动主链路。
- **配置化优先**：Prompt、召回策略、业务规则尽量配置化而非硬编码。运营要调整推荐策略，改配置即可，不需要发版。
- **变更分级**：区分"方向性变更"（需要重新评估方案）和"参数性变更"（调个阈值/改个文案），后者直接改配置上线。
- **每周对齐**：和产品固定时间对齐需求优先级，减少临时插入。

## 12. 请举例说明你如何平衡功能实现和技术债务。

**场景：** 导购项目一期赶上线，知识库管理用了最简单的文件导入 + MySQL 存储，没做版本管理和审核流程。

**当时的判断：**
- 一期核心目标是验证 Agent 导购的业务效果（GMV 增量），知识库管理的体验不影响 A/B 实验结果
- 把工时花在 Agent 核心链路的稳定性和效果上，ROI 更高
- 记录了技术债：无版本管理、无审核流程、无批量更新能力

**后续偿还：**
- A/B 验证成功后，二期专门排了一个迭代补齐知识库管理平台
- 加了版本管理、审核流程、批量导入/导出、变更回滚

**原则：技术债不是不能欠，关键是「知道自己欠了什么、什么时候还、不还的风险是什么」。**

---

# 第 13-24 题

## 13. 你如何设计一个可扩展的 Agent 架构？

我们的实际设计：

```
用户输入 → 安全风控 → 意图分类器
  → 简单意图 → 固定 DAG（LangGraph 静态图）
  → 复杂意图 → Plan & Execute（Planner → Executor → Re-planner）
```

**可扩展性体现在：**

1. **意图层可扩展**：新增业务场景 = 新增意图分类标签 + 对应的处理链路，不改主框架。
2. **工具层可扩展**：所有工具（商品检索、知识库查询、Text2SQL 等）统一接口（输入 schema + 输出 schema），新增工具只需注册，Planner 自动感知。
3. **Plan 模板可扩展**：简单意图的固定 Plan 配置化管理，运营可自行编排。
4. **子 Agent 可扩展**：复杂场景的子 Agent 独立开发部署，通过 RPC 调用，主 Agent 只关心输入输出契约。
5. **状态管理统一**：LangGraph 的 StateGraph 统一管理对话状态，新增节点不影响已有状态流转。

## 14. 在团队中，你如何推动技术规范的落地？

**四步：先做样板 → 自动化 → 文档化 → 定期 review。**

1. **自己先做样板**：写第一个符合规范的模块，让团队看到"规范落地后的代码长什么样"。
2. **自动化卡点**：能用工具检查的绝不靠人工。代码风格用 lint，提交信息用 commitlint，CR 时用 checklist 模板。
3. **文档简洁可查**：规范文档控制在 1-2 页，写 do/don't 示例，不写长篇大论。
4. **CR 中 coach**：代码 review 时遇到不符合规范的，不只说"不行"，而是说"这样改 + 为什么这样更好"。

## 15. 请分享一次你通过监控发现并解决问题的经历。

**场景：** 导购上线后某天下午，监控报警 Agent RT P99 飙到 3s（正常 300ms）。

**排查过程：**
1. 看监控大盘，发现是商品向量检索环节耗时暴增
2. 查 Milvus 监控，发现 QPS 正常但延迟飙升
3. 进一步查发现是某个 collection 正在做后台 compaction，占用了大量 IO
4. 临时措施：将该 collection 的 compaction 调度窗口改到凌晨低峰期
5. 长期措施：向量库读写分离，查询走只读副本，compaction 在写入节点执行

**收获：** 之后在所有依赖的中间件上加了资源隔离策略的 checklist。

## 16. 你如何评估不同技术方案的优劣？

**评估框架（五个维度打分）：**

1. **效果达标度**：能否满足业务需求和质量指标？（一票否决项）
2. **工程复杂度**：开发周期、维护成本、团队学习曲线
3. **性能**：RT、吞吐、资源消耗
4. **可扩展性**：未来需求变化时改动范围
5. **风险**：技术成熟度、社区活跃度、是否有 fallback 方案

实际例子：选 LangGraph vs AutoGen vs CrewAI 时——
- LangGraph：状态管理精细、可控性强、LangChain 生态、但学习曲线略陡
- AutoGen：多 Agent 对话模式好，但对执行流程的精细控制弱
- CrewAI：上手简单，但 re-plan 和异常处理能力不足

最终选 LangGraph，核心决策因素是"复杂场景的可控性"，这在电商导购中是刚需。

## 17. 当产品经理提出不合理需求时，你会怎么沟通？

**原则：不说"做不了"，说"这样做的代价是什么"，让对方自己判断是否值得。**

1. **先确认目标**：理解 PM 提这个需求背后真正想解决的问题，有可能需求不合理但问题是真实的。
2. **量化代价**：如果直接实现需要 X 人天、影响 Y 功能的进度、带来 Z 风险，列出来。
3. **提供替代方案**：给出一个成本更低但能解决同一问题 80% 的方案。
4. **让数据说话**：如果有历史数据证明这个方向不可行，拿出来。

例子：PM 要求 Agent 能处理图片输入（用户拍照找同款），但当时多模态模型的准确率和 RT 都不达标。我拉了一组测试数据演示效果，PM 自己看完就同意先上文字版。

## 18. 描述一次你处理线上 Agent 故障的完整过程。

**故障：** 某次发版后，Agent 对部分用户的回复变成空内容。

**处理过程：**
1. **止血（5min）**：监控报警触发，看到回复空率从 0.1% 飙到 15%。立即回滚到上一版本，空率恢复。
2. **定位（30min）**：对比新旧版本 diff，发现新版修改了 Prompt 模板，在特定意图分支下 Prompt 拼接逻辑有 bug，导致发给 LLM 的 prompt 格式错误，LLM 返回空。
3. **修复（20min）**：修复 Prompt 拼接逻辑，增加输出非空校验（如果 LLM 返回空则走兜底话术）。
4. **验证**：在测试环境用故障 case 复现并验证修复，然后灰度发布。
5. **复盘**：增加发版前的 Prompt 回归测试（用 golden set 跑一遍所有意图分支确认输出非空且格式正确）。

## 19. 你如何确保 Agent 在不同场景下的稳定性？

**多层保障：**

1. **降级兜底**：每个环节都有降级策略。LLM 超时 → 走规则引擎；向量检索失败 → 走关键词搜索；全部失败 → 返回"抱歉，请稍后再试"+ 转人工。
2. **输入校验**：安全风控前置拦截（注入攻击、敏感内容），意图分类加"兜底"类别处理无法识别的输入。
3. **输出校验**：回复内容过滤（价格信息交叉验证、禁止虚假承诺）、非空校验、长度校验。
4. **超时控制**：每个环节设独立超时，总链路有全局超时，避免某个工具卡死拖垮全链路。
5. **流量隔离**：不同场景走不同的 LLM 模型实例，防止互相影响。
6. **压测**：上线前模拟峰值 2 倍流量压测，验证各环节的水位。

## 20. 请举例说明你如何优化 Agent 的内存使用。

**场景：** Agent 服务在高并发下内存占用持续增长，触发 OOM。

**排查与优化：**
1. **内存 profiling** 发现对话历史缓存是大头——每个 session 缓存完整对话历史，长对话 session 的内存占用线性增长。
2. **优化措施**：
   - 对话历史改为滑动窗口（只保留最近 N 轮），超出部分持久化到 Redis/DB
   - 早期 summary：超过阈值时先用 LLM 总结前文，用 summary 替代完整历史
   - 向量检索结果不缓存原始文档，只缓存 ID + 摘要
3. **LangGraph 状态管理**：checkpoint 使用外部存储（Redis）而非内存，状态按需加载。
4. **效果**：单实例内存从 8GB 降到 2GB，同时支撑的并发 session 数提升 4 倍。

## 21. 在技术选型时，你主要考虑哪些因素？

和第 16 题互补，更全面的框架：

1. **业务匹配度**：能否满足当前和可预见的未来需求
2. **团队能力**：团队的学习成本，是否有人有经验
3. **社区与生态**：文档质量、社区活跃度、issue 响应速度
4. **性能与成本**：满足 SLA 的前提下总拥有成本（TCO）最低
5. **可替代性**：万一选错了，迁移成本有多大？尽量不锁死在某个技术上
6. **公司技术栈兼容性**：能否和现有基础设施（监控、部署、CI/CD）无缝集成

## 22. 你如何培养团队的技术氛围？

- **技术分享常态化**：每两周一次 30 分钟 tech talk，轮流主讲，主题不限（新技术调研、踩坑经验、论文解读）。
- **Code Review 文化**：CR 不是挑刺，是学习机会。鼓励在 CR 中分享"为什么这样写更好"。
- **新人 onboarding**：让新人在第一周就提交一个有意义的 PR，建立信心。
- **技术决策透明**：重要技术决策写 RFC，全团队可以 comment，让每个人都有参与感。
- **以身作则**：自己保持学习，分享调研结论，展示"学习是日常而不是额外负担"。

## 23. 请分享一次你成功说服他人采纳你技术方案的经历。

**场景：** 导购项目选型时，另一位同事倾向用 AutoGen 做多 Agent 编排（上手快、demo 效果好），我认为应该用 LangGraph。

**说服过程：**
1. 没有直接否定 AutoGen，而是提出一个具体的业务场景："用户问搭配推荐，中间某个品类库存不足需要 re-plan"。
2. 分别用 AutoGen 和 LangGraph 实现这个场景的 demo。
3. AutoGen 的实现需要大量 hack（因为它的多 Agent 对话模式不原生支持中途 re-plan），LangGraph 的 StateGraph 天然支持条件分支和状态回溯。
4. 把两个 demo 的代码量、可读性、扩展难度对比展示。

**结果：** 同事看完 demo 对比后主动同意用 LangGraph。关键不是口头说服，而是用代码证明。

## 24. 你如何设计 Agent 的容错机制？

**分层容错：**

1. **输入层**：安全风控拦截恶意输入、意图分类加兜底类别（unknown → 转人工或通用回复）。
2. **执行层**：
   - 每个工具调用有超时 + 重试（最多 2 次，指数退避）
   - 工具调用失败 → Re-planner 重新规划绕过失败的工具
   - 整体执行超时 → 返回已有的部分结果 + 兜底话术
3. **输出层**：
   - LLM 输出格式校验（JSON schema validation），格式错误触发重试
   - 内容安全过滤（价格交叉验证、敏感信息检测）
   - 空回复检测 → 兜底话术
4. **状态层**：LangGraph checkpoint 支持断点恢复，中途失败可从上一个 checkpoint 继续。
5. **系统层**：核心依赖（LLM、向量库、搜索）都有降级方案，单点故障不会导致全链路不可用。

---

# 第 25-36 题

## 25. 请详细描述你设计的一个多 Agent 协作系统架构。

就是淘工厂 ChatBot 导购系统：

```
用户输入
  → SafeGuard Agent（安全风控：注入检测、敏感词、合规）
  → Intent Router（意图分类：SFT 小模型）
    ├─ 简单意图（FAQ/物流查询/价格查询）
    │   → 固定 DAG：Intent → Tool Call → Response Generator
    └─ 复杂意图（搭配推荐/多轮比价/需求模糊）
        → Planner Agent：生成 [(sub_agent, tool)] 执行计划
        → Executor：逐步执行
        │   ├─ Retrieval Agent（多路召回：向量/SQL/行为/Graph）
        │   ├─ Knowledge Agent（知识库 QA：反向 HyDE + RAG）
        │   ├─ Ranking Agent（相关性精排 + 业务规则过滤）
        │   └─ Response Agent（结果聚合 + 话术生成）
        → Re-planner：检查中间结果，决定继续/修改/终止
```

**协作机制：**
- 状态通过 LangGraph StateGraph 统一管理，每个 Agent 读写同一份状态
- Agent 间通过状态传递数据（不是直接调用），解耦依赖
- Planner 是唯一的调度者，子 Agent 不会互相调用

## 26. 当 Agent 出现幻觉问题时，你有哪些应对策略？

**多层防御：**

1. **源头控制**：
   - 知识库质量严格把关（结构化挖掘 + 人工审核），垃圾进垃圾出
   - Prompt 明确指令："如果知识库中没有相关信息，回复「暂无相关信息」，不要编造"
2. **召回约束**：
   - 设置相关性阈值，低于阈值的召回结果不送入 LLM
   - 商品推荐时，属性词输出严格约束在离线沉淀的标准化词库范围内
3. **输出校验**：
   - 价格信息与商品数据库交叉验证，不一致则过滤
   - 关键事实（库存、发货时间、退换政策）从数据库直接读取拼接，不让 LLM 生成
4. **用户反馈闭环**：
   - 用户可标记"回答不准确"，触发人工审核并回流到训练数据

## 27. 你如何设计 Agent 的长期记忆和上下文管理？

**三层记忆架构：**

1. **短期记忆（对话上下文）**：LangGraph StateGraph 管理当前 session 的对话历史，滑动窗口保留最近 N 轮，超出部分用 LLM summary 压缩。
2. **中期记忆（Session 级）**：用户一次访问中的偏好（"我要便宜的""不要红色"），存在 session state 中，对话结束释放。
3. **长期记忆（用户画像）**：跨 session 的用户偏好，使用 Mem0 持久化。包括购买历史、品类偏好、价格敏感度等。Agent 每次对话开始时加载用户画像作为 context。

**上下文窗口管理：**
- Token 预算分配：system prompt（固定）+ 用户画像（固定）+ 对话历史（动态）+ 当前 query + 召回结果
- 当总 token 超预算时，优先压缩对话历史（summary），其次裁剪召回结果数量

## 28. 在 Agent 开发中，你如何处理用户意图识别不准确的问题？

**系统性方法：**

1. **评测先行**：建立 golden test set（500+ 标注样本），覆盖各意图类别和边界 case。每次改动必须在 test set 上回归。
2. **错误分析**：对分类错误的 case 做系统性分析，分类原因（标签定义模糊？训练数据不足？query 过短？多意图？）。
3. **针对性优化**：
   - 标签定义模糊 → 重新定义意图边界，合并或拆分类别
   - 训练数据不足 → 数据增强（同义改写、LLM 生成变体）
   - Query 过短 → 结合对话上下文做意图补全
   - 多意图 → 支持多标签分类，按优先级处理
4. **兜底策略**：置信度低于阈值时，主动追问（"您是想了解商品价格还是想看搭配推荐？"）而不是猜。
5. **持续迭代**：线上 bad case 自动收集 → 人工标注 → 回流训练集 → SFT 更新。

## 29. 请分享一个你优化 Agent 推理速度的具体案例。

和第 10 题互补，聚焦 LLM 推理本身的优化：

**背景：** Plan 生成环节用 GPT-4 级别模型，单次推理 1.2s。

**优化手段：**
1. **Prompt 精简**：砍掉冗余 few-shot examples（从 8 个减到 3 个，选最有代表性的），token 数减少 60%。
2. **KV Cache**：system prompt + few-shot 部分是固定的，启用 prefix caching，避免每次重新计算。
3. **模型降级**：对简单意图（占 80%），Plan 生成用小模型（GPT-3.5 级别），只有复杂意图用大模型。
4. **结构化输出**：让 LLM 直接输出 JSON 格式的 Plan，避免自由文本解析的额外步骤。
5. **并行化**：多路召回并行执行，Re-plan 判断和下一步执行 overlap。

**效果：** Plan 生成 RT 从 1.2s 降到 200ms（简单意图）/ 500ms（复杂意图）。

## 30. 你如何设计 Agent 的工具调用机制？

**设计原则：统一接口、声明式注册、安全隔离。**

```python
# 统一工具接口
class Tool:
    name: str                    # 工具名
    description: str             # 给 LLM 看的描述
    input_schema: JsonSchema     # 输入参数 schema
    output_schema: JsonSchema    # 输出 schema
    
    def execute(self, params: dict) -> ToolResult:
        ...
```

**关键设计：**
1. **声明式注册**：工具通过配置注册到 ToolRegistry，Planner 从 registry 获取可用工具列表和描述，无需硬编码。
2. **输入校验**：调用前用 JSON Schema 校验参数，防止 LLM 输出的参数格式错误传到工具。
3. **超时与重试**：每个工具配置独立的超时时间和重试策略。
4. **权限控制**：不同意图只能访问对应的工具子集（如售前咨询不能调用下单工具）。
5. **日志与追踪**：每次工具调用记录输入、输出、耗时、状态，支持全链路追踪。
6. **Mock 支持**：测试时可注入 mock 工具，不依赖真实外部服务。

## 31. 当 Agent 需要处理复杂任务拆解时，你的设计思路是什么？

核心就是 **Plan & Execute**：

1. **Planner 生成执行计划**：接收用户意图 + 上下文 + 可用工具列表，输出 `List[Step]`，每个 Step 指定 `(sub_agent, tool, expected_output)`。
2. **执行粒度下沉**：Plan 粒度到 `[(agent, tool)]` 级别，而不是只到 agent 级别，因为工具间有前后依赖（比如先检索商品 ID，再用 ID 查价格）。
3. **中间结果校验**：每步执行后检查结果是否符合预期，不符合则触发 Re-plan。
4. **Re-plan 机制**：Re-planner 拿到中间结果，判断是继续原计划、修改后续步骤、还是终止并返回部分结果。
5. **最大步数限制**：防止无限循环，设置最大执行步数（如 10 步），超出则强制返回已有结果。

## 32. 请举例说明你如何提升 Agent 的对话连贯性。

**场景：** 用户多轮对话中，Agent 回答前后矛盾或忘记之前说过的内容。

**解决方案：**
1. **对话状态显式管理**：在 StateGraph 中维护 `resolved_constraints`（已确认的约束，如"预算 500 以内""不要红色"），每轮对话更新。后续回复必须满足所有已确认约束。
2. **指代消解**：用户说"那个""上面那个"时，结合对话历史做指代消解后再执行。
3. **一致性检查**：回复生成前，校验推荐结果是否与之前的承诺一致（之前说有货，这次不能说没货）。
4. **摘要注入**：长对话时将之前的摘要注入 prompt，确保 LLM 有足够上下文。

## 33. 你如何设计 Agent 的自我评估和修正机制？

**三层机制：**

1. **执行时自检**：每步工具调用后，检查输出是否合理（非空、格式正确、数值在合理范围内）。不合理则重试或跳过。
2. **回复前校验**：生成回复后，用一个轻量级的 validator（规则 + 小模型）检查：
   - 是否回答了用户的问题（相关性）
   - 是否包含幻觉（与召回结果交叉验证）
   - 是否违反安全规则
   - 校验不通过则 regenerate 或走兜底
3. **事后评估**：线上回复按一定比例抽样，用 LLM 多维打分（准确性、相关性、安全性、流畅度），分数低的 case 自动标记进入人工审核队列，修正后回流训练集。

## 34. 在 Agent 安全方面，你有哪些实践经验？

**安全是前置关卡，不是事后补救。**

1. **Prompt 注入防护**：
   - 输入预处理：检测并过滤常见注入模式（"ignore previous instructions""你现在是..."）
   - System prompt 与 user prompt 隔离，不拼接在一起
2. **输出安全过滤**：
   - 敏感信息脱敏（手机号、地址等不在回复中暴露）
   - 禁止生成虚假承诺（"保证正品""假一赔十"等需要从审核通过的话术库中引用）
3. **权限最小化**：Agent 只能调用白名单内的工具，不能执行写操作（下单、退款等高风险操作需转人工确认）。
4. **限流防刷**：单用户对话频率限制，防止恶意用户大量调用消耗 LLM 资源。
5. **审计日志**：所有对话记录留痕，支持事后审计和溯源。

## 35. 请描述一个你设计的 Agent 技能学习框架。

**"评测驱动、数据闭环"的持续学习框架：**

```
线上对话 → 抽样评测 → Bad case 收集 → 人工标注
  → 数据增强 → SFT 微调 → 离线评测（golden set）
  → 灰度发布 → 线上 A/B → 全量
```

**核心组件：**
1. **四维评测体系**：测试集自动评测 + 代码规则校验 + 人工标注 + LLM 多维打分。
2. **Bad case 自动挖掘**：用户负反馈（点"不满意"）、低置信度回复、异常对话轮次过多的 session 自动标记。
3. **数据飞轮**：bad case → 修正标注 → 回流训练集 → 模型更新 → 线上效果提升 → 更少 bad case。
4. **能力版本管理**：每次模型更新记录版本号、训练数据量、评测指标，支持回滚。

## 36. 你如何平衡 Agent 的通用能力和垂直领域专精？

**策略：通用底座 + 垂直增强层。**

1. **通用底座**：使用通用大模型作为基础能力（语言理解、推理、生成），不从零训练。
2. **垂直增强**：
   - **SFT 微调**：用电商导购场景的标注数据微调意图分类和 Plan 生成
   - **知识库注入**：领域知识通过 RAG 注入，不烧进模型权重，方便更新
   - **工具特化**：工具层是纯领域逻辑（商品检索、价格比较、库存查询），与通用 LLM 能力解耦
3. **切换成本低**：如果要迁移到另一个垂直领域（比如从服装导购到 3C 导购），只需替换知识库 + 工具 + 少量 SFT 数据，底座和架构不变。

---

# 第 37-48 题

## 37. 当 Agent 面对开放式问题时，你的处理策略是什么？

**核心策略：收敛 → 确认 → 执行。**

用户说"帮我推荐点好的"——这是典型的开放式问题，没有明确品类、预算、用途。

1. **主动追问收敛**：不猜测，而是问关键约束。"您想看哪个品类的商品？有预算要求吗？"最多追问 2 轮。
2. **利用画像补全**：如果有用户历史数据（购买记录、浏览偏好），用画像作为默认约束，同时告知用户："根据您之前的浏览，为您推荐了几款夏季连衣裙，如果不是您想要的，可以告诉我更具体的需求。"
3. **兜底推荐**：如果既无历史数据也问不出约束，走热销/新品推荐作为兜底，总比不回复好。

**设计原则：** Agent 不应该在信息不足时强行做决策，但也不应该无限追问让用户烦躁。2 轮追问是平衡点。

## 38. 请分享一次你优化 Agent 知识检索效率的经历。

**背景：** 售前咨询 RAG 召回率只有 45%，用户问"这个会不会起球"，知识库里有答案但检索不到。

**分析原因：** 用户 query 和知识库文档在语义空间差距大——用户用口语化表达，知识库是结构化的 Q&A。

**优化方案：反向 HyDE**

传统 HyDE 是用 LLM 先生成一个假文档再用假文档做检索。但在我们场景下假文档质量不稳定。

反向思路：不动文档侧，而是将检索空间迁移到 query 侧——
1. 知识库中每条 Q&A 离线生成多个"用户可能的问法"变体（LLM 改写）
2. 将这些变体和原始 Q&A 一起建入向量索引
3. 在线检索时，用户 query 与变体匹配的概率大幅提升

**效果：** 召回率从 45% 提升到 78%。后续配合相关性精排，整体准确率达到 85%+。

## 39. 你如何设计 Agent 的个性化适配能力？

**三个层次的个性化：**

1. **用户画像驱动**：长期记忆存储用户偏好（品类、价位、风格），每次对话加载画像作为 context，推荐结果优先匹配偏好。
2. **行为实时适配**：当前 session 内，根据用户的点击/跳过行为实时调整推荐策略。点了便宜的 → 后续推荐偏低价；跳过了红色 → 后续过滤红色。
3. **表达风格适配**：根据用户对话风格调整回复风格（简洁型用户给简洁回复，详细型用户给详细回复）。通过 prompt 中注入风格指令实现。

## 40. 在 Agent 多轮对话中，你如何管理对话状态？

**使用 LangGraph StateGraph 统一管理：**

```python
class ConversationState(TypedDict):
    messages: list           # 对话历史
    intent: str              # 当前意图
    constraints: dict        # 已确认的约束条件
    plan: list               # 当前执行计划
    step_index: int          # 当前执行到第几步
    intermediate_results: list  # 中间结果
    user_profile: dict       # 用户画像
```

**关键设计：**
- **状态持久化**：通过 LangGraph checkpoint 机制，状态持久化到 Redis，支持断点恢复。
- **状态合并**：多个子 Agent 并行执行后，结果合并回主状态。
- **状态清理**：session 超时自动清理，防止内存泄漏。
- **状态版本**：每次状态变更生成新版本，支持回溯（调试用）。

## 41. 请举例说明你如何提升 Agent 的规划能力。

**从"规划不准"到"规划准确"的迭代过程：**

初版 Planner 直接让 LLM 生成执行计划，问题：
- 经常遗漏步骤（比如忘了做相关性过滤就直接返回结果）
- 步骤顺序错误（先排序再召回）
- 生成无意义的步骤

**优化措施：**
1. **Plan 模板库**：针对高频意图预定义 Plan 模板，Planner 从模板库选择并微调，而非从零生成。
2. **Few-shot 精选**：挑选 3 个最有代表性的 Plan 示例放入 prompt，覆盖简单/中等/复杂三种难度。
3. **约束注入**：在 prompt 中明确规则——"必须先召回再排序""召回结果必须经过相关性过滤""最多 5 步"。
4. **SFT 微调**：用人工标注的 300+ (query, plan) 对微调 Planner 模型。
5. **执行反馈学习**：Plan 执行失败的 case 自动收集，分析失败原因后补充到训练数据。

## 42. 你如何设计 Agent 的反馈学习机制？

**数据飞轮闭环：**

```
线上服务 → 用户反馈（显式：点赞/踩；隐式：点击/购买/跳出）
  → 数据标注管道（自动 + 人工）
  → 训练数据更新
  → 模型微调（SFT）
  → 离线评测（golden set + 新增 case）
  → 灰度验证
  → 全量上线
```

**关键设计：**
- **隐式反馈比显式更重要**：用户很少点"不满意"，但点击率和购买率是最真实的反馈。推荐了但没点击 → 可能推荐不准。
- **负样本挖掘**：低置信度 + 用户无点击的回复自动标记为待审核。
- **防止退化**：每次更新后在 golden set 上回归，指标不能下降。

## 43. 当 Agent 需要处理多模态输入时，你的架构设计思路？

**渐进式架构：**

1. **统一输入协议**：定义 `MultiModalInput`，支持 text / image / audio 等类型，每种类型有对应的预处理器。
2. **模态转换层**：非文本模态先转换为文本表示。图片 → 多模态 LLM 生成描述 + 属性提取；语音 → ASR 转文字。
3. **统一文本处理**：转换后进入统一的意图分类 → Plan & Execute 流程，下游不感知原始模态。
4. **模态特异性保留**：图片搜索同款场景，除了文本描述外还保留图片 embedding，走图搜图链路并行召回。

**实际考量：** 多模态模型的准确率和 RT 目前还不如纯文本链路稳定，所以要设计降级方案——多模态处理失败时引导用户用文字描述。

## 44. 请分享一个你设计的 Agent 评估指标体系。

**四维评测体系：**

| 维度 | 指标 | 方法 |
|------|------|------|
| **自动化测试集** | 意图准确率、Plan 准确率、回复相关性 | Golden set 500+ case，每次发版回归 |
| **代码规则校验** | 格式合规率、安全合规率、字段完整性 | 规则引擎自动检查 LLM 输出 |
| **人工标注** | 回复质量评分（1-5 分）、幻觉率 | 每周抽样 100 条人工评估 |
| **LLM 多维打分** | 准确性/相关性/安全性/流畅度/有用性 | GPT-4 作为 judge，5 个维度分别打分 |

**业务指标：**
- 推荐点击率、购买转化率、客单价、GMV 增量
- 对话满意度、转人工率（越低越好）、平均对话轮次

## 45. 你如何实现 Agent 的持续学习和优化？

和第 42 题互补，更偏工程视角：

1. **数据管道自动化**：线上日志 → Flink 实时流处理 → 自动标签（高/低质量）→ 数据仓库。
2. **定期微调**：每两周用新增标注数据做一轮 SFT，自动跑评测对比新旧模型。
3. **A/B 灰度**：新模型先在 10% 流量验证，指标达标后逐步放量。
4. **知识库动态更新**：商品上下架、价格变动、活动信息自动同步到知识库，无需人工干预。
5. **Prompt 版本管理**：所有 prompt 有版本号，支持灰度和回滚。

## 46. 在 Agent 部署上线后，你如何进行效果监控？

**三层监控：**

1. **技术指标（实时）**：
   - RT P50/P99、QPS、错误率、超时率
   - LLM token 消耗、各工具调用耗时
   - Grafana 大盘 + 阈值报警
2. **质量指标（准实时）**：
   - 回复空率、格式错误率、降级触发率
   - 意图分类置信度分布
   - 每小时自动抽样用 LLM judge 打分
3. **业务指标（T+1）**：
   - 推荐 CTR、CVR、GMV
   - 对话满意度、转人工率
   - 对比基线的 A/B 指标

**报警策略：** 技术指标秒级报警；质量指标分钟级报警（空率>5%触发）；业务指标 T+1 日报 review。

## 47. 请描述你设计的一个 Agent 任务编排系统。

就是 Plan & Execute 编排系统的工程实现：

**核心组件：**
1. **Plan Registry**：维护所有可用的 Plan 模板和动态 Plan 生成能力。
2. **Task Queue**：Plan 生成的步骤序列进入 Task Queue，Executor 按序消费。
3. **Executor Pool**：子 Agent 池，每个子 Agent 封装一类能力（检索/排序/生成），可水平扩展。
4. **State Store**：Redis 存储执行状态，支持断点恢复。
5. **Re-plan Trigger**：中间结果校验器，触发条件包括：工具返回空、结果不满足约束、超时。

**执行流程：** Planner → Task Queue → Executor 逐步执行 → 每步结果写入 State Store → Re-plan Trigger 检查 → 通过则继续 / 不通过则 Re-plan → 全部完成 → Response Agent 聚合生成回复。

## 48. 你认为未来 Agent 技术发展的关键方向是什么？

几个趋势：

1. **长期记忆与个性化**：当前 Agent 大多是无状态对话，未来需要真正的长期记忆，让 Agent 越用越懂用户。Mem0 这类项目是早期探索。
2. **多 Agent 协作标准化**：目前多 Agent 编排各家实现不同（LangGraph / AutoGen / CrewAI），未来会收敛出标准协议和框架。
3. **推理与规划能力**：当前 Planner 依赖 LLM 生成计划，准确率有限。专门针对规划能力的训练和强化学习会是重点方向。
4. **端侧 Agent**：模型小型化让 Agent 可以在端侧运行，降低延迟和成本，保护隐私。
5. **可信与可控**：幻觉控制、安全对齐、可解释性。尤其在电商/金融等对准确性要求高的场景，可信赖是落地前提。
6. **工具使用能力的泛化**：让 Agent 能自动发现和学习使用新工具，而不是依赖人工注册。


